<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <!-- Open Graph meta -->
        {%- include 'og-meta.njk' %}

        <!-- Twitter meta -->
        {%- include 'twitter-meta.njk' %}

        <title>{{ siteData.title }} &mdash; {{ siteData.subTitle }}</title>
        <link rel="stylesheet" href="{{ 'css/styles.css' | url }}" />
    </head>
    <body>
        {{ content | safe }}
        <script type="module" src="{{ 'js/bundle.esm.js' | url }}"></script>
        <script nomodule src="{{ 'js/bundle.js' | url }}"></script>
    </body>
</html>
